spring:
  application:
    name: nemi-report
  profiles:
    active: dev, custom-view

  config:
    import:
      - optional:configserver:http://nemi-centralized-configuration-service:8888
      - optional:aws-parameterstore:/config/${spring.application.name}_dev/
      - optional:aws-parameterstore:/config/srs-nemi-tool_dev/
