spring:
  application:
    name: nemi-report
  profiles:
    active: dev

  config:
    import:
      - optional:configserver:http://nemi-centralized-configuration-service:8888
      - optional:aws-parameterstore:/config/${spring.application.name}_${spring.profiles.active:default}/
      - optional:aws-parameterstore:/config/srs-nemi-tool_${spring.profiles.active:default}/
